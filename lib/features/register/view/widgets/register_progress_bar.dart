import 'package:flutter/material.dart';

class RegisterProgressBar extends StatelessWidget {
  const RegisterProgressBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
